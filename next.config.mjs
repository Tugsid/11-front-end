/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "i.pinimg.com",
      "media1.tenor.com",
      "media.tenor.com",
      "aniyuki.com",
    ],
  },
};

export default nextConfig;